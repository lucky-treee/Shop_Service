spring:
  application:
    name: shop-service
  profiles:
    active: main
  config:
    import: "optional:configserver:http://localhost:8888/"
  cloud:
    config:
      name: eureka, actuator, shopService
      profile: develop
server:
  port: 10300
